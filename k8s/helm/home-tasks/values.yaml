# Default values for home-tasks

replicaCount: 1

image:
  repository: ghcr.io/bender-916/home-tasks
  pullPolicy: Always
  tag: "latest"

# For private registries, create a secret with:
# kubectl create secret docker-registry ghcr-secret --docker-server=ghcr.io --docker-username=USERNAME --docker-password=TOKEN
imagePullSecrets: []
  # - name: ghcr-secret
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: false

podAnnotations: {}

podSecurityContext:
  fsGroup: 999

securityContext:
  runAsNonRoot: true
  runAsUser: 999
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "ovh"
  hosts:
    - host: home-task.plasm.es
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: home-task-tls
      hosts:
        - home-task.plasm.es

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 128Mi

persistence:
  enabled: true
  size: 1Gi
  storageClass: ""
  accessMode: ReadWriteOnce

config:
  flaskEnv: production
  logLevel: INFO

secrets:
  secretKey: "change-me-in-production"

nodeSelector: {}
tolerations: []
affinity: {}
